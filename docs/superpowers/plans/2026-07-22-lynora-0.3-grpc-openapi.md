# Lynora 0.3 — gRPC + OpenAPI/Proto Import

**Goal:** OpenAPI 3 JSON import, `.proto` import into request stubs, and unary gRPC send via dynamic protobuf (prost-reflect + tonic).

**Tasks:** openapi import · proto import · grpc unary module · Protocol::Grpc · desktop UI hooks · tests · push
