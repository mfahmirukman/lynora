# Lynora 0.5 — CLI + WebSockets/SSE

**Goal:** `lynora` CLI runs collections in CI (exit codes + JSON/text reports; optional `expectStatus`). Core + desktop support WebSocket and SSE sessions.

**Stack:** clap for CLI; `tokio-tungstenite` for WS; reqwest byte stream + SSE parser for SSE.
